# Database Configuration
DATABASE_URL=postgresql://user:password@host:5432/database

# JWT Configuration (REQUIRED - Generate a secure random string)
# Example: openssl rand -base64 32
JWT_SECRET=your-secure-random-secret-key-change-this-in-production

# Frontend URL for CORS
FRONTEND_URL=http://localhost:5173

# Server Configuration
PORT=3001
NODE_ENV=development

# Optional: Email Configuration (for user invitations)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_FROM=noreply@assureproai.com

# Optional: SSO Configuration
# SAML_CERT_PATH=/path/to/cert.pem
# OAUTH_CLIENT_ID=your-oauth-client-id
# OAUTH_CLIENT_SECRET=your-oauth-client-secret

# Production Settings (for Render deployment)
# NODE_ENV=production
# DATABASE_URL will be automatically set by Render
# JWT_SECRET should be set in Render environment variables
# FRONTEND_URL should be set to your production frontend URL
